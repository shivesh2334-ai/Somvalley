import os

vercel_config = '''{
  "version": 2,
  "name": "somvalley-play-school",
  "builds": [
    {
      "src": "index.html",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}'''

output_path = '/home/claude/output/vercel.json'
os.makedirs(os.path.dirname(output_path), exist_ok=True)  # ← key fix

with open(output_path, 'w') as f:
    f.write(vercel_config)
